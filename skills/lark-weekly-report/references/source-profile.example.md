# 本地周报配置示例

复制为同目录 `source-profile.local.md` 后填写；不要提交真实配置。

- 报告作者显示名：
- 标题格式：姓名工作周报（M.D-M.D）
- 时区：Asia/Shanghai
- 标准模板 URL（只读）：
- 个人工作母版 URL（可选，只读）：
- 历史写作样例 URL（只读）：
- OKR 图片与格式参考 URL（可选，只读）：
- 排期 Base URL（只读）：
- Base 标题及排期表名称：
- 项目群名称：
- 当前周报存放位置：
- 已确认的固定会议及星期：
- 已确认的跨项目审核、初版交付职责与日期字段：
- OKR 大目标分组（仅填已确认内容）：
- 是否沿用 Rainbow 三列布局及个人写作约定：

资源仍需执行时重新核验。不要填写 token、密钥或固定用户 open_id。每周写入目标由当前请求或经核实的当周目录确定。
